apiVersion: apps/v1
kind: Deployment
metadata:
  name: livestream-frontend-deployment
  namespace: tenant-7654b5-asrpods
  labels:
    app: test-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: livestream-frontend
  template:
    metadata:
      labels:
        app: livestream-frontend
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.coreweave.cloud/cpu
                    operator: In
                    values:
                      - intel-xeon-v3
      containers:
        - name: frontend
          image: exartadev/zeniva.exarta:livestream-frontend-v1-asrpods

          ports:
            - containerPort: 5173
          resources:
            requests:
              memory: 100Mi
            limits:
              memory: 1Gi
          imagePullPolicy: Always
      imagePullSecrets:
        - name: dockerhub-secret
      restartPolicy: Always